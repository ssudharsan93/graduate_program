This folder contains a
  -Makefile
  -main.cpp source file
  -TreeNode.cpp source file
  -TreeNode.h header file

In order to compile the files in this folder,
and create executables, you simply run the
`make` command on the command line. `make clean`
will get rid of executables and source files.

In order to run the executable file
'hw2_bst_delete' simply run `./hw2_bst_delete` 
on the command line to run the  binary search tree
algorithm for inserting and deleting the nodes provided
in the homework.